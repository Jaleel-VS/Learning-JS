let petName = "Popeye"
// It's illegal to define more than one once
// let petName = "Spot"

/*We're not defining a new variable, we're just a new value
to the existing variable */
petName = "Spot"

console.log(petName)

//2. There are rules related to variables names
/* - variable has to start with letter, underscore or dollar sign
 - Can't use exclamation mark
 - can't be reserved keywords */